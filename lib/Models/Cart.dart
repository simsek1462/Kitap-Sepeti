class Cart{
  String key;
  String userId;
  String bookId;
  String title;
  String author;
  String url;
  String price;
  String content;

  Cart(this.key, this.userId, this.bookId, this.title, this.author, this.url,
      this.price, this.content);

}